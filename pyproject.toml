[project]
name = "light-obsidian-mcp"
version = "0.2.0"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]",
]

[project.scripts]
light-obsidian-mcp = "light_obsidian_mcp.server:main"
